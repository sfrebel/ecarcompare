<%= form_with(model: car, local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :car_name %>
  </div>

  <div class="field">
    <%= form.label :base_price %>
    <%= form.text_field :base_price, id: :car_base_price %>
  </div>

  <div class="field">
    <%= form.label :battery_capacity %>
    <%= form.text_field :battery_capacity, id: :car_battery_capacity %>
  </div>

  <div class="field">
    <%= form.label :range %>
    <%= form.text_field :range, id: :car_range %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
